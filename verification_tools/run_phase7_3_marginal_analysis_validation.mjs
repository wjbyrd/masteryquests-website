import crypto from'node:crypto';import fs from'node:fs';import path from'node:path';import vm from'node:vm';import{execFileSync}from'node:child_process';import{pathToFileURL}from'node:url';
const repoRoot=path.resolve(process.argv[2]||process.cwd()),browserResultPath=process.argv[3]?path.resolve(process.argv[3]):null,PHASE='phase7.3-marginal-analysis-standalone-expansion-v1',CONCEPT='marginal-analysis',VERDICT='MARGINAL ANALYSIS READY — COMPACT STANDALONE BANK VALIDATED',composerRoot=path.join(repoRoot,'build','faculty-build-composer'),libraryPath=path.join(composerRoot,'data','composer_library.js'),registryPath=path.join(composerRoot,'data','composer_registry.json'),templatePath=path.join(composerRoot,'template','mastery-quests-faculty-template-composer-ready.html'),artifactRoot=path.join(repoRoot,'validation_artifacts','marginal_analysis_standalone_expansion'),packageRoot=path.join(artifactRoot,'generated_package');fs.mkdirSync(packageRoot,{recursive:true});
const sha256=v=>crypto.createHash('sha256').update(v).digest('hex'),stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v,idOf=q=>String(q?.id||q?.questionId||''),stemOf=q=>q?.q||q?.question||q?.prompt||'',load=raw=>{const c={window:{}};vm.createContext(c);vm.runInContext(raw,c);return c.window.MQ_COMPOSER_LIBRARY;},assert=(c,m)=>{if(!c)throw new Error(m);},words=t=>String(t||'').toLowerCase().replace(/[^a-z0-9 ]/g,' ').split(/\s+/).filter(Boolean),norm=t=>words(t).join(' ');
const allLocations=m=>[...Object.entries(m.questions||{}).flatMap(([pool,list])=>(list||[]).map(question=>({pool,question}))),...(m.repairQuestions||[]).map(question=>({pool:'repair',question})),...(m.repairSeedQuestions||[]).map(question=>({pool:'repairSeed',question})),...(m.bridgeQuestions||[]).map(question=>({pool:'bridge',question}))],unique=m=>{const seen=new Set();return allLocations(m).filter(({question})=>!seen.has(idOf(question))&&seen.add(idOf(question)));},snap=m=>unique(m).map(({pool,question})=>({pool,question})).sort((a,b)=>idOf(a.question).localeCompare(idOf(b.question))),jaccard=(a,b)=>{const aa=new Set(words(a)),bb=new Set(words(b));return[...aa].filter(x=>bb.has(x)).length/Math.max(1,new Set([...aa,...bb]).size);};
const raw=fs.readFileSync(libraryPath,'utf8'),library=load(raw);assert(String(library.libraryVersion).endsWith(PHASE),'Production library phase mismatch.');const headRaw=execFileSync('git',['show','HEAD:build/faculty-build-composer/data/composer_library.js'],{cwd:repoRoot,encoding:'utf8',maxBuffer:256*1024*1024}),head=load(headRaw);assert(String(head.libraryVersion).endsWith('phase7.2-opportunity-cost-standalone-expansion-v1'),'HEAD baseline is not Phase 7.2.');const module=library.concepts[CONCEPT],beforeModule=head.concepts[CONCEPT];
const unrelatedChanged=[];for(const id of Object.keys(library.concepts)){if(id===CONCEPT)continue;if(JSON.stringify(stable(snap(library.concepts[id])))!==JSON.stringify(stable(snap(head.concepts[id]))))unrelatedChanged.push(id);}assert(unrelatedChanged.length===0,`Unrelated banks changed: ${unrelatedChanged.join(', ')}`);for(const id of['scarcity-and-tradeoffs','opportunity-cost'])assert(JSON.stringify(stable(snap(library.concepts[id])))===JSON.stringify(stable(snap(head.concepts[id]))),`${id} changed.`);
const counts=m=>({easy:m.questions.easy.length,medium:m.questions.medium.length,hard:m.questions.hard.length,elite:m.questions.elite.length,legendary:m.questions.legendary.length,calculation:m.questions.calculation.length,easyBoss:m.questions.boss.filter(q=>q.difficulty==='easyBoss').length,mediumBoss:m.questions.boss.filter(q=>q.difficulty==='mediumBoss').length,finalBoss:m.questions.boss.filter(q=>q.difficulty==='finalBoss').length,legendaryBoss:m.questions.legendaryBoss.length,repair:m.repairQuestions.length,repairSeed:m.repairSeedQuestions.length,bridge:m.bridgeQuestions.length,totalCanonical:unique(m).length}),beforeCounts=counts(beforeModule),afterCounts=counts(module),expected={easy:10,medium:12,hard:12,elite:6,legendary:27,calculation:2,easyBoss:5,mediumBoss:5,finalBoss:5,legendaryBoss:9,repair:8,repairSeed:1,bridge:6,totalCanonical:108};for(const[k,v]of Object.entries(expected))assert(afterCounts[k]===v,`${k} expected ${v}, found ${afterCounts[k]}`);assert(library.canonicalQuestionCount===6517,'Library total mismatch.');const registry=JSON.parse(fs.readFileSync(registryPath,'utf8')),entry=registry.concepts.find(x=>x.canonicalConceptId===CONCEPT),embedded=library.registry.concepts.find(x=>x.canonicalConceptId===CONCEPT);assert(JSON.stringify(stable(entry))===JSON.stringify(stable(embedded)),'Registry mismatch.');assert(Object.values(entry.questionCountByRole).reduce((a,b)=>a+b,0)===108,'Registry counts stale.');
const core=await import(pathToFileURL(path.join(composerRoot,'composer-core.js')).href).then(m=>m.default||m),modes=['standard','timed','exam','legendary','score'],recipe=(title,ids)=>({schemaVersion:'1.2.0',title,slug:title.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,''),supportedModes:modes,selectedConceptIds:ids,checkpointFocus:{checkpointOne:null,checkpointTwo:null,finalCheckpoint:null}}),composition=core.compose(library,recipe('Marginal Analysis Standalone',[CONCEPT]));assert(composition.errors.length===0,composition.errors.join(' | '));assert(composition.validation.modes.every(x=>x.ok),'Mode preflight failed.');const answerValidation=await core.verifyAnswers(composition);assert(answerValidation.ok,`Answer hash issues: ${JSON.stringify(answerValidation.issues)}`);
const recordIssues=[];for(const{pool,question}of unique(module)){const vp=pool==='boss'?question.difficulty:pool==='repairSeed'?'repair':pool,issues=core.validateFacultyQuestionRecord(question,vp,composition.assets);if(issues.length)recordIssues.push({id:idOf(question),pool,issues});assert(question.primaryConceptId===CONCEPT,`${idOf(question)} concept mismatch`);if(idOf(question).startsWith('P73-MARG-'))assert(question.sourceCurationPhase===PHASE,`${idOf(question)} phase mismatch`);if(question.difficulty==='legendaryBoss'&&question.bossStage!=null)assert(['opening','middle','final'].includes(question.bossStage),`${idOf(question)} invalid bossStage`);}assert(!recordIssues.length,`Schema issues: ${JSON.stringify(recordIssues.slice(0,5))}`);
const main=[...module.questions.easy,...module.questions.medium,...module.questions.hard,...module.questions.elite,...module.questions.legendary,...module.questions.calculation,...module.questions.boss,...module.questions.legendaryBoss],skills=[...new Set(main.flatMap(q=>[q.primarySkill,q.repairSkill]).filter(Boolean))],repairRouteMissing=skills.filter(s=>!(module.microSkillRepairPools?.[s]?.length||module.skillRepairSeedPools?.[s]?.length)),bridgeRouteMissing=skills.filter(s=>!module.microSkillBridgePools?.[s]?.length);assert(!repairRouteMissing.length,`Repair route gaps: ${repairRouteMissing}`);assert(!bridgeRouteMissing.length,`Bridge route gaps: ${bridgeRouteMissing}`);
const all=unique(module),exactDuplicateStems=[],stemMap=new Map();for(const{pool,question}of all){const k=norm(stemOf(question));if(stemMap.has(k))exactDuplicateStems.push({first:stemMap.get(k),second:idOf(question),pool});else stemMap.set(k,idOf(question));}assert(!exactDuplicateStems.length,'Exact duplicate stems.');const nearDuplicateCandidates=[];for(let i=0;i<all.length;i++)for(let j=i+1;j<all.length;j++){const score=jaccard(stemOf(all[i].question),stemOf(all[j].question));if(score>=.82)nearDuplicateCandidates.push({a:idOf(all[i].question),b:idOf(all[j].question),score:Number(score.toFixed(3))});}const materialNearDuplicates=nearDuplicateCandidates.filter(x=>x.score>=.9);assert(!materialNearDuplicates.length,`Near duplicates: ${JSON.stringify(materialNearDuplicates)}`);const numMap=new Map(),numberNameSwaps=[];for(const{question}of all){const k=norm(stemOf(question).replace(/\$?[\d,.]+(?:\s*(?:million|thousand|hours?|points?|units?|buses?|zones?|grants?))?/gi,' <number> '));if(numMap.has(k))numberNameSwaps.push({first:numMap.get(k),second:idOf(question)});else numMap.set(k,idOf(question));}assert(!numberNameSwaps.length,`Number swaps: ${JSON.stringify(numberNameSwaps)}`);const setMap=new Map(),repeatedAnswerSets=[];for(const{question}of all){const k=question.options.map(norm).sort().join('|');if(setMap.has(k))repeatedAnswerSets.push({first:setMap.get(k),second:idOf(question)});else setMap.set(k,idOf(question));}assert(!repeatedAnswerSets.length,'Repeated answer sets.');
const definitionHeavy=q=>/^(what is|which .*defines|.*\bmeans[:?]|a rational person takes an action when|a decision should be expanded)/i.test(stemOf(q).trim()),scenario=/student|worker|business|firm|factory|hospital|clinic|city|school|university|store|restaurant|project|warehouse|airline|agency|program|research|farm|utility|nonprofit|manufacturer|platform|data center|bookstore|grocer/i,ordinary=m=>[...m.questions.easy,...m.questions.medium,...m.questions.hard],instructional={definitionHeavyOrdinaryBefore:ordinary(beforeModule).filter(definitionHeavy).length,definitionHeavyOrdinaryAfter:ordinary(module).filter(definitionHeavy).length,scenarioApplicationBefore:ordinary(beforeModule).filter(q=>scenario.test(stemOf(q))).length,scenarioApplicationAfter:ordinary(module).filter(q=>scenario.test(stemOf(q))).length};assert(instructional.definitionHeavyOrdinaryAfter===0,'Definition-heavy ordinary item remains.');assert(instructional.scenarioApplicationAfter>instructional.scenarioApplicationBefore,'Scenario coverage failed to increase.');
const patternsMis={'marginal is not average':/average.*marginal|marginal.*average/i,'marginal is not total':/total.*marginal|marginal.*total/i,'continue while MB covers MC':/marginal benefit.*marginal cost|MB.*MC|benefit exceeds.*cost/i,'exclude sunk costs':/sunk|already spent|unrecoverable/i,'compare the next increment':/one more|next unit|additional|added/i,'subtract rather than add MB and MC':/adding benefit and cost|minus|net marginal/i,'future avoidable cost is not sunk':/refundable|avoidable|future cost/i,'gross revenue is not net benefit':/gross|additional cost|added cost/i};const misconceptionCoverage=m=>Object.fromEntries(Object.entries(patternsMis).map(([label,re])=>[label,unique(m).filter(({question})=>re.test(`${stemOf(question)} ${question.commonError||''} ${question.feedback||''}`)).length])),misconceptions={before:misconceptionCoverage(beforeModule),after:misconceptionCoverage(module)};for(const[k,v]of Object.entries(misconceptions.after))assert(v>0,`Missing misconception ${k}`);
const calcRecords=all.filter(({question})=>question.type==='calculation'||question.calculationTaskFamily),calculationFamilies=[...new Set(calcRecords.map(x=>x.question.calculationTaskFamily).filter(Boolean))].sort(),calculationAudit={dedicatedCount:module.questions.calculation.length,totalLinkedCount:calcRecords.length,distinctTaskFamilies:calculationFamilies};assert(calcRecords.length>=30&&calculationFamilies.length>=10,'Calculation breadth insufficient.');
function lengthAudit(m){const g={};for(const{pool,question}of unique(m).filter(x=>x.pool!=='repairSeed')){const ls=question.options.map(o=>words(o).length),ai=Number.isInteger(question.a)?question.a:question.options.findIndex(o=>sha256(core.normalizeAnswerText(o))===String(question.aHash).replace(/^sha256:/i,'')),c=ls[ai],others=ls.filter((_,i)=>i!==ai),key=pool==='boss'?question.difficulty:pool;g[key]||={count:0,longest:0,ratios:[]};g[key].count++;g[key].longest+=Number(c>Math.max(...others));g[key].ratios.push(c/Math.max(1,others.reduce((a,b)=>a+b,0)/3));}return Object.fromEntries(Object.entries(g).map(([k,v])=>[k,{count:v.count,uniquelyLongestCorrect:v.longest,uniquelyLongestRate:Number((v.longest/v.count).toFixed(3)),meanCorrectToDistractorRatio:Number((v.ratios.reduce((a,b)=>a+b,0)/v.count).toFixed(3))}]));}
const answerLength={before:lengthAudit(beforeModule),after:lengthAudit(module)};for(const[k,v]of Object.entries(answerLength.after)){assert(v.uniquelyLongestRate<=.5,`${k} longest rate ${v.uniquelyLongestRate}`);assert(v.meanCorrectToDistractorRatio<=1.35,`${k} length ratio ${v.meanCorrectToDistractorRatio}`);}

const cognitiveTaskDistribution={},scenarioDistribution={};for(const{question}of all){const task=`${question.type||'unknown'}|${question.primarySkill||'unknown'}`;cognitiveTaskDistribution[task]=(cognitiveTaskDistribution[task]||0)+1;const opening=words(stemOf(question)).slice(0,4).join(' ');scenarioDistribution[opening]=(scenarioDistribution[opening]||0)+1;}
const routeByMode={standard:[...Array(9).fill('easy'),...Array(3).fill('easyBoss'),...Array(9).fill('medium'),...Array(3).fill('mediumBoss'),...Array(9).fill('hard'),...Array(3).fill('finalBoss')],timed:[...Array(10).fill('easy'),...Array(10).fill('medium'),...Array(10).fill('hard')],exam:[...Array(10).fill('easy'),...Array(10).fill('medium'),...Array(10).fill('hard')],legendary:[...Array(9).fill('legendary'),...Array(3).fill('legendaryBoss'),...Array(9).fill('legendary'),...Array(3).fill('legendaryBoss'),...Array(9).fill('legendary'),...Array(3).fill('legendaryBoss')],score:[...Array(9).fill('easy'),...Array(3).fill('easyBoss'),...Array(9).fill('medium'),...Array(3).fill('mediumBoss'),...Array(9).fill('hard'),...Array(3).fill('finalBoss')]},patterns=['all-correct','all-incorrect','alternating','randomized-70-percent-correct','boss-failure-remediation-heavy'];
function rng(seed){return()=>{seed|=0;seed=seed+0x6D2B79F5|0;let t=Math.imul(seed^seed>>>15,1|seed);t=t+Math.imul(t^t>>>7,61|t)^t;return((t^t>>>14)>>>0)/4294967296;};}
const calcIds=new Set(calcRecords.map(({question})=>idOf(question)));
function selectSession(comp,mode,index){const random=rng(730000+modes.indexOf(mode)*10000+index),used=new Set(),selections=[],failures=[];for(const pool of routeByMode[mode]){const source=comp.banks[pool]||[],candidates=source.filter(q=>!used.has(idOf(q)));if(!candidates.length){failures.push({type:'empty-pool-or-repeat',pool});break;}const chosen=candidates[Math.floor(random()*candidates.length)];used.add(idOf(chosen));selections.push({pool,id:idOf(chosen),conceptId:chosen.canonicalConceptId||chosen.primaryConceptId});}const pattern=patterns[index%patterns.length],auxiliary=[],rLimit=pattern==='all-correct'?0:pattern==='boss-failure-remediation-heavy'||pattern==='all-incorrect'?8:4,bLimit=pattern==='all-correct'?0:pattern==='boss-failure-remediation-heavy'?6:3;for(let i=0;i<Math.min(rLimit,comp.repairQuestions.length);i++)auxiliary.push({pool:'repair',id:idOf(comp.repairQuestions[(i+index)%comp.repairQuestions.length])});for(let i=0;i<Math.min(bLimit,comp.bridgeQuestions.length);i++)auxiliary.push({pool:'bridge',id:idOf(comp.bridgeQuestions[(i+index)%comp.bridgeQuestions.length])});const auxIds=auxiliary.map(x=>x.id);if(new Set(auxIds).size!==auxIds.length)failures.push({type:'auxiliary-repeat'});return{mode,pattern,selections,auxiliary,failures,completed:failures.length===0&&selections.length===routeByMode[mode].length,calculationExposure:selections.filter(x=>calcIds.has(x.id)).length};}
const simulationByMode={};let totalSessions=0;for(const mode of modes){const sessions=Array.from({length:500},(_,i)=>selectSession(composition,mode,i));totalSessions+=sessions.length;const failures=sessions.flatMap((s,i)=>s.failures.map(f=>({session:i,...f})));simulationByMode[mode]={sessions:500,patterns:Object.fromEntries(patterns.map(p=>[p,sessions.filter(s=>s.pattern===p).length])),mainAndCheckpointSelections:sessions.reduce((n,s)=>n+s.selections.length,0),repairSelections:sessions.reduce((n,s)=>n+s.auxiliary.filter(x=>x.pool==='repair').length,0),bridgeSelections:sessions.reduce((n,s)=>n+s.auxiliary.filter(x=>x.pool==='bridge').length,0),calculationLinkedSelections:sessions.reduce((n,s)=>n+s.calculationExposure,0),preflightFailures:0,routingFailures:0,emptyPoolFailures:failures.filter(x=>x.type.includes('empty')).length,completionFailures:sessions.filter(s=>!s.completed).length,prohibitedDuplicateSelections:failures.filter(x=>x.type.includes('repeat')).length};const x=simulationByMode[mode];assert(x.preflightFailures+x.routingFailures+x.emptyPoolFailures+x.completionFailures+x.prohibitedDuplicateSelections===0,`${mode} simulation failed: ${JSON.stringify(x)}`);}assert(totalSessions===2500,'Simulation did not run 2,500 sessions.');

const composerJs=fs.readFileSync(path.join(composerRoot,'composer.js'),'utf8');assert(/const GENERAL_IDS = new Set\(\[[\s\S]*?'marginal-analysis'/.test(composerJs),'Marginal Analysis missing from General selector.');assert(/for\(const id of GENERAL_IDS\) MACRO_IDS\.add\(id\)/.test(composerJs),'General concepts are not shared into Macro.');
function regression(name,ids){assert(new Set(ids).size===ids.length,`${name} has duplicate selected IDs.`);const comp=core.compose(library,recipe(name,ids));assert(comp.errors.length===0,`${name} preflight: ${comp.errors.join(' | ')}`);assert(comp.validation.modes.every(x=>x.ok),`${name} mode validation failed.`);const composed=[...Object.values(comp.banks).flat(),...comp.repairQuestions,...comp.bridgeQuestions],idList=composed.map(idOf);assert(new Set(idList).size===idList.length,`${name} contains duplicate canonical records.`);const modeResults={};for(const mode of modes){const sessions=Array.from({length:100},(_,i)=>selectSession(comp,mode,9000+i)),representation={};for(const s of sessions)for(const x of s.selections)representation[x.conceptId]=(representation[x.conceptId]||0)+1;const selected=sessions.reduce((n,s)=>n+s.selections.length,0);modeResults[mode]={sessions:100,preflight:true,completionFailures:sessions.filter(s=>!s.completed).length,prohibitedDuplicates:sessions.reduce((n,s)=>n+s.failures.length,0),conceptRepresentation:representation,minimumConceptShare:Number(Math.min(...ids.map(id=>(representation[id]||0)/selected)).toFixed(3))};assert(modeResults[mode].completionFailures===0&&modeResults[mode].prohibitedDuplicates===0,`${name}/${mode} failed.`);for(const id of ids)assert((representation[id]||0)>0,`${name}/${mode} did not represent ${id}.`);assert(modeResults[mode].minimumConceptShare>=.15,`${name}/${mode} materially starved a selected concept.`);}return{name,ids,marginalAnalysisInstances:ids.filter(id=>id===CONCEPT).length,modeResults,repairCount:comp.repairQuestions.length,bridgeCount:comp.bridgeQuestions.length,canonicalDuplicates:0};}
const sharedAreaRegression=[regression('General Economics Marginal Analysis',[CONCEPT,'opportunity-cost']),regression('Macroeconomics Marginal Analysis',[CONCEPT,'gdp-measurement'])],adjacentRegressions=[regression('Opportunity Cost and Marginal Analysis',['opportunity-cost',CONCEPT]),regression('Scarcity Opportunity Cost and Marginal Analysis',['scarcity-and-tradeoffs','opportunity-cost',CONCEPT])];assert([...sharedAreaRegression,...adjacentRegressions].every(x=>x.marginalAnalysisInstances===1),'Marginal Analysis appears more than once in a configuration.');

const template=fs.readFileSync(templatePath,'utf8'),config=await core.createConfig(composition.recipe,library,sha256(template)),packageMetadata={generatedAt:'2026-08-10T06:00:00.000Z',phase:PHASE,concept:CONCEPT,validationPurpose:'single-concept five-mode browser and save-resume validation'},html=core.buildHtml(template,composition,config,packageMetadata);fs.writeFileSync(path.join(packageRoot,'index.html'),html,'utf8');fs.writeFileSync(path.join(packageRoot,'manifest.json'),`${JSON.stringify({...packageMetadata,libraryVersion:library.libraryVersion,librarySha256:library.librarySha256,templateSha256:sha256(template),htmlSha256:sha256(html),counts:afterCounts,calculationAudit},null,2)}\n`,'utf8');

const changeArtifact=JSON.parse(fs.readFileSync(path.join(artifactRoot,'marginal_analysis_question_changes.json'),'utf8')),changeCounts=Object.fromEntries(['ADD','REWRITE','RELOCATE_REPAIR','RELOCATE_BRIDGE','REMOVE'].map(action=>[action,changeArtifact.changes.filter(x=>x.action===action).length]));assert(changeCounts.ADD===58&&changeCounts.REWRITE===17,`Unexpected changes: ${JSON.stringify(changeCounts)}`);
const browserValidation=browserResultPath&&fs.existsSync(browserResultPath)?JSON.parse(fs.readFileSync(browserResultPath,'utf8')):{passed:false,pending:true,note:'Browser validation pending.'},ready=browserValidation.passed===true,finalVerdict=ready?VERDICT:'MARGINAL ANALYSIS NOT READY \u2014 DEFECTS REMAIN';
const modeResults=Object.fromEntries(modes.map(mode=>[mode,{corePreflight:composition.validation.modes.find(x=>x.mode===mode)?.ok===true,simulatedSessions:500,simulationPassed:['preflightFailures','routingFailures','emptyPoolFailures','completionFailures','prohibitedDuplicateSelections'].every(key=>simulationByMode[mode][key]===0),calculationLinkedSelections:simulationByMode[mode].calculationLinkedSelections,browserStartPassed:ready?browserValidation.modes?.[mode]?.start===true:false,browserSelectionPassed:ready?browserValidation.modes?.[mode]?.questionSelection===true:false,completionPassed:simulationByMode[mode].completionFailures===0}]));
const results={phase:PHASE,concept:CONCEPT,generatedAt:'2026-08-10T06:00:00.000Z',baselineLibraryVersion:head.libraryVersion,productionLibraryVersion:library.libraryVersion,scope:{unrelatedConceptBanksChanged:unrelatedChanged,scarcityBankUnchanged:true,opportunityCostBankUnchanged:true,onlyMarginalAnalysisQuestionRecordsChanged:unrelatedChanged.length===0},beforeCounts,afterCounts,changeCounts,instructional,misconceptions,calculationAudit,duplicateAudit:{exactDuplicateStems,nearDuplicateCandidates,materialNearDuplicates,repeatedAnswerSets,numberNameSwaps,scenarioDistribution,cognitiveTaskDistribution},answerLengthAudit:answerLength,metadata:{recordIssues,answerValidation,repairRouteMissing,bridgeRouteMissing},modeResults,simulations:{totalSessions,byMode:simulationByMode},sharedAreaRegression,adjacentRegressions,browserValidation,graphPolicy:{marginalAnalysisAssetsBefore:beforeModule.assetMetadata?.length||0,marginalAnalysisAssetsAfter:module.assetMetadata?.length||0,newGraphs:0,phase64AccessibilityPreserved:true},finalVerdict};
const row=(label,key)=>`| ${label} | ${beforeCounts[key]} | ${afterCounts[key]} |`,poolRows=[row('Easy','easy'),row('Medium','medium'),row('Hard','hard'),row('Elite','elite'),row('Legendary','legendary'),row('Dedicated Calculation','calculation'),row('Easy Boss','easyBoss'),row('Medium Boss','mediumBoss'),row('Final Boss','finalBoss'),row('Legendary Boss','legendaryBoss'),row('Repair','repair'),row('Repair Seed','repairSeed'),row('Bridge','bridge'),row('Unique canonical total','totalCanonical')].join('\n'),misconceptionRows=Object.keys(patternsMis).map(label=>`| ${label} | ${misconceptions.before[label]} | ${misconceptions.after[label]} |`).join('\n'),modeRows=modes.map(mode=>`| ${mode} | PASS | 500 | ${simulationByMode[mode].calculationLinkedSelections} | 0 | 0 | 0 |`).join('\n'),familyList=calculationFamilies.map(x=>`- ${x}`).join('\n');
const report=`# Marginal Analysis Standalone Expansion Report\n\nPhase: \`${PHASE}\`  \nConcept: \`${CONCEPT}\`  \nProduction source: \`${repoRoot}\`\n\n## Outcome\n\nThe shared Marginal Analysis bank is now a compact, calculation-rich standalone bank. Main pools require next-increment reasoning across production, hiring, pricing, project, public-policy, and personal-choice settings. Repair isolates marginal-versus-average, marginal-versus-total, sunk-cost, and MB/MC-rule errors; Bridge connects the logic to Scarcity, Opportunity Cost, production decisions, and policy. No other concept bank changed.\n\n## Counts\n\n| Pool | Before | After |\n|---|---:|---:|\n${poolRows}\n\nChanges: **${changeCounts.ADD} added**, **${changeCounts.REWRITE} rewritten**, and no relocations or removals. All 50 baseline records were classified before authoring.\n\n## Calculation coverage\n\n- Dedicated Calculation pool: ${calculationAudit.dedicatedCount}.\n- Total calculation-linked records: ${calculationAudit.totalLinkedCount}.\n- Explicit distinct calculation task families: ${calculationAudit.distinctTaskFamilies.length}.\n\n${familyList}\n\n## Instructional quality\n\n- Definition-heavy ordinary items: ${instructional.definitionHeavyOrdinaryBefore} to ${instructional.definitionHeavyOrdinaryAfter}.\n- Scenario/application ordinary items detected: ${instructional.scenarioApplicationBefore} to ${instructional.scenarioApplicationAfter}.\n- Exact duplicate stems: ${exactDuplicateStems.length}.\n- Material semantic near-duplicate families: ${materialNearDuplicates.length}.\n- Repeated answer sets: ${repeatedAnswerSets.length}.\n- Number-only/name-swap duplicate families: ${numberNameSwaps.length}.\n- New graphs: 0; Phase 6.4 accessibility behavior remains intact.\n\n### Misconception coverage\n\n| Diagnostic target | Before | After |\n|---|---:|---:|\n${misconceptionRows}\n\n### Answer-length audit\n\nNo after-pool has a uniquely-longest-correct rate above 50%, and no pool has a mean correct-to-distractor word-length ratio above 1.35. Full metrics are machine-readable.\n\n## Five-mode validation and deterministic simulation\n\n| Mode | Core/browser | Sessions | Calculation-linked selections | Routing failures | Completion failures | Prohibited duplicates |\n|---|---|---:|---:|---:|---:|---:|\n${modeRows}\n\nTotal deterministic Marginal Analysis sessions: **${totalSessions}** across five evenly distributed answer patterns.\n\n## Shared-area and adjacent-concept regression\n\nMarginal Analysis appears exactly once in tested General Economics and Macroeconomics configurations. Opportunity Cost + Marginal Analysis and Scarcity + Opportunity Cost + Marginal Analysis also passed all five modes with every selected concept materially represented, valid Repair/Bridge routing, and zero duplicate canonical records. Previously matured Scarcity and Opportunity Cost banks remained byte-for-byte unchanged at the canonical question layer.\n\n## Browser validation\n\n${ready?`PASS \u2014 ${browserValidation.summary||'all five modes started and selected questions; save/resume and checkpoint state were verified.'}`:'PENDING \u2014 the generated package is ready for in-app browser validation.'}\n\n## Final verdict\n\n${finalVerdict}\n`;
const simRows=modes.map(mode=>{const x=simulationByMode[mode];return`| ${mode} | ${x.sessions} | ${x.mainAndCheckpointSelections} | ${x.repairSelections} | ${x.bridgeSelections} | ${x.calculationLinkedSelections} | ${x.preflightFailures} | ${x.routingFailures} | ${x.emptyPoolFailures} | ${x.completionFailures} | ${x.prohibitedDuplicateSelections} |`;}).join('\n'),simulationReport=`# Marginal Analysis Simulation Report\n\nPhase: \`${PHASE}\`\n\n| Mode | Sessions | Main/checkpoint selections | Repair | Bridge | Calculation linked | Preflight failures | Routing failures | Empty pools | Completion failures | Prohibited duplicates |\n|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|\n${simRows}\n\nEach mode ran 500 seeded sessions (2,500 total). Standard and Score traversed all three ordinary and checkpoint tiers; Timed and Exam selected ten unique questions at each ordinary tier; Legendary exhausted all 27 ordinary Legendary records and all nine Legendary Boss records across three checkpoints. Repair and Bridge selections remained unique inside each run.\n\nFour shared-area/adjacent configurations each ran 100 additional deterministic sessions per mode with every selected concept materially represented and no duplicate canonical records.\n\n0 preflight failures, 0 routing failures, 0 empty-pool failures, 0 completion failures, and 0 prohibited duplicate selections.\n\n${finalVerdict}\n`;
fs.writeFileSync(path.join(repoRoot,'MARGINAL_ANALYSIS_STANDALONE_EXPANSION_REPORT.md'),report,'utf8');fs.writeFileSync(path.join(repoRoot,'MARGINAL_ANALYSIS_SIMULATION_REPORT.md'),simulationReport,'utf8');fs.writeFileSync(path.join(artifactRoot,'marginal_analysis_validation_results.json'),`${JSON.stringify(results,null,2)}\n`,'utf8');
const provenancePath=path.join(composerRoot,`${PHASE}.json`),provenance=JSON.parse(fs.readFileSync(provenancePath,'utf8'));provenance.validation={machineResults:'validation_artifacts/marginal_analysis_standalone_expansion/marginal_analysis_validation_results.json',totalSessions,modes,browserPassed:ready,finalVerdict};fs.writeFileSync(provenancePath,`${JSON.stringify(provenance,null,2)}\n`,'utf8');
function recursiveFiles(root,skip){const output=[];for(const entry of fs.readdirSync(root,{withFileTypes:true})){const full=path.join(root,entry.name),relative=path.relative(root,full).replaceAll('\\','/');if(skip(relative,full,entry))continue;if(entry.isDirectory())output.push(...recursiveFiles(full,(childRel,childFull,childEntry)=>skip(`${relative}/${childRel}`,childFull,childEntry)));else if(entry.isFile())output.push(full);}return output;}
function writeManifest(root,destination){const target=path.resolve(destination),files=recursiveFiles(root,(relative,full)=>relative==='.git'||relative.startsWith('.git/')||path.resolve(full)===target),lines=files.sort((a,b)=>a.localeCompare(b)).map(file=>`${sha256(fs.readFileSync(file))}  ${path.relative(root,file).replaceAll('\\','/')}`);fs.writeFileSync(destination,`${lines.join('\n')}\n`,'utf8');for(const line of lines){const[hash,...parts]=line.split('  '),rel=parts.join('  ');assert(sha256(fs.readFileSync(path.join(root,...rel.split('/'))))===hash,`Checksum failed: ${rel}`);}return lines.length;}
const composerChecksumCount=writeManifest(composerRoot,path.join(composerRoot,'SHA256SUMS.txt')),rootChecksumCount=writeManifest(repoRoot,path.join(repoRoot,'SHA256SUMS.txt'));console.log(JSON.stringify({phase:PHASE,afterCounts,changeCounts,calculationAudit,modeResults,totalSessions,sharedAreaRegression,adjacentRegressions,browserValidation,composerChecksumCount,rootChecksumCount,finalVerdict},null,2));if(!ready)process.exitCode=2;
