'use strict';
const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
const root=path.resolve(__dirname,'../..'),work=path.join(root,'tmp/microeconomics_exception_closure'),read=f=>JSON.parse(fs.readFileSync(path.join(work,f),'utf8'));
const expected=read('expected_changes.json'),targets=read('target_checks.json'),publication=read('publication.json'),exportsCheck=read('export_checks.json'),companions=read('companion_exports.json'),visual=read('visual_review.json'),hashes=read('hash_comparison.json');
const suiteLog=fs.readFileSync(path.join(work,'active_suite.log'),'utf8'),exportTests=fs.readFileSync(path.join(work,'exporter_tests.log'),'utf8');
const runners=[...suiteLog.matchAll(/^PASS (.+)$/gm)].map(m=>({runner:m[1].trim(),status:'PASS'}));assert.equal(runners.length,29);assert.match(exportTests,/Ran 14 tests/);assert.match(exportTests,/\bOK\b/);assert.equal(targets.status,'PASS');assert.equal(publication.questions,6276);assert.equal(publication.priorPublishedCount,6276);assert.equal(publication.revisedIdsPublished,3);assert(visual.passed);
for(const k of ['csv_mismatches','structure_errors','pdf_blank_pages','pdf_replacement_characters','pdf_bounds_errors','pdf_render_errors','pdf_answer_marker_errors','pdf_split_cores','pdf_missing_image_pages','pdf_link_errors'])assert.equal(exportsCheck[k].length,0,k);
assert.equal(exportsCheck.csv_rows,6301);assert.equal(exportsCheck.csv_unique_ids,6301);assert.equal(exportsCheck.pdf_question_ids,6301);assert(exportsCheck.pdf_id_order_matches);
for(const c of Object.values(companions)){assert.equal(c.rows,c.count);assert.equal(c.unique,c.count);assert.equal(c.mismatches.length,0);}
assert(!hashes.some(x=>x.file.startsWith('faculty_exports/audits/')));
const exceptions={'P62B-ELAS-B3-019':'MFV-001','PM5-PC-BR-088':'MEA-0078 remaining bridge exception','P62C-CPS-H-026':'MFV-003'};
const actions={'P62B-ELAS-B3-019':'Supplied fixed positive quantity and rising price directly; removed absent-graph wording.','PM5-PC-BR-088':'Used the repaired price-fall conclusion to select incumbent output from a short MC schedule.','P62C-CPS-H-026':'Mapped approved accounting-profit content to existing Costs of Production / profit-concepts metadata.'};
const changes=expected.changes.map(c=>({...c,exception:exceptions[c.id],status:'CLOSED',action:actions[c.id]}));
const ledger={title:'Microeconomics Post-Verification Exception Closure',date:'2026-09-26',startingStatus:'PASS WITH DOCUMENTED EXCEPTIONS',finalStatus:'PASS',baselineRef:expected.baselineRef,baselineSourceSha256:expected.baselineSourceSha256,afterLibrarySha256:expected.afterLibrarySha256,changedQuestionIds:expected.changedQuestionIds,changedQuestionCount:3,unchangedQuestionCount:9776,changes,moves:expected.moves,projection:{before:{...expected.countsBefore,global:9779},after:{...expected.countsAfter,global:9779},membershipUnchanged:true,sharedGeneralTargets:[]},validation:{runners,exporterTests:{total:14,passed:14},targets,publication,exports:Object.fromEntries(Object.entries(exportsCheck).filter(([k])=>!['ids','question_pages'].includes(k))),companions:Object.fromEntries(Object.entries(companions).map(([k,v])=>[k,Object.fromEntries(Object.entries(v).filter(([f])=>f!=='ids'))])),visual},protectedReportsUnchanged:true,trackedFileChanges:hashes,remainingQuestionLevelExceptions:0,unchangedNonblockingLimitations:['MODE-AVAILABILITY','LEGACY-PUBLICATION','PDF-METADATA-CONTINUATIONS']};
const accounting=changes.find(c=>c.id==='P62C-CPS-H-026'),fmt=v=>Array.isArray(v)?v.join(', '):String(v),lines=[];
function add(s=''){lines.push(s);}
add('# Microeconomics Post-Verification Exception Closure\n');
add('## Starting Status\n');add('Reference: [microeconomics_final_verification.md](microeconomics_final_verification.md). Starting status: **PASS WITH DOCUMENTED EXCEPTIONS**.\n');
add('The exact current records, corresponding verification exceptions, and cleanup-ledger entries were inspected and their before states saved before editing. All three exceptions were present. Immutable baseline: `'+expected.baselineRef+'`; canonical source SHA-256: `'+expected.baselineSourceSha256+'`.\n');
add('## Scope\n');add('Exactly three canonical IDs changed: P62B-ELAS-B3-019, PM5-PC-BR-088, and P62C-CPS-H-026. All other **9,776** records are unchanged. No question was added or deleted. No polished-game bank was used or edited. No broader content audit was performed. The complete before/after records and field-level changes are in [microeconomics_exception_closure_changes.json](microeconomics_exception_closure_changes.json).\n');
add('## Exception Closure Table\n');add('Question ID | Verification exception | Action taken | Fields changed | Final status\n--- | --- | --- | --- | ---');for(const c of changes)add(`${c.id} | ${c.exception} | ${c.action} | ${c.fields.join(', ')} | CLOSED`);add();
add('## Bridge Transfer Check\n');
add('Item | Skill | Task\n--- | --- | ---\nP62F-PC-R-017 (unchanged repair) | entry | Positive economic profit causes entry; identify that market supply rises and price falls.\nPM5-PC-BR-088 (revised bridge) | entry | Given entry and the price fall from $10 to $6, use MC values $3, $5, $8, $12 to determine that incumbent output falls from 3 units to 2.\n');
add('**New transfer step:** apply the lower market price to the price-taking firm\'s marginal output decision. Repeating “supply rises and price falls” cannot identify the correct output pair; the student must compare each price with the supplied MC schedule. The repair conclusion is supplied in the bridge so the additional demand is one application step. The four choices represent the correct adjustment, unchanged output despite a new price, confusion between market supply and incumbent output, and unnecessary shutdown.\n');
add('Both records retain primarySkill/repairSkill `entry`. The bridge retains its bridge role and medium canonical difficulty. Every direct repair, repair-seed, skill-repair, and skill-bridge map remains exactly unchanged. P62F-PC-R-017 remains in the existing `repair_entry_exit` pool; PM5-PC-BR-088 remains in the `entry` bridge pool. No new route or skill was introduced.\n');
add('## Metadata Alignment Check\n');add('For P62C-CPS-H-026:\n\nField | Before | After\n--- | --- | ---');for(const f of accounting.fields)add(`${f} | ${fmt(accounting.before[f])} | ${fmt(accounting.after[f])}`);add();
add('Existing objective COP.1: “Distinguish explicit cost, implicit cost, accounting profit, economic profit, and normal profit.” Existing faculty outcome `profit-concepts-coverage` includes `accounting_profit`. The existing accounting-profit repair pool resolves to **PMC-COP-R-023**, which corrects the mistake of subtracting an owner\'s forgone salary from accounting profit. No dangling route or duplicate skill was introduced.\n');
add('The record moved from the Consumer/Producer Surplus hard pool to the Costs of Production hard pool. Its resolved `profit-concepts` view now includes it, and `producer-surplus` excludes it. Micro membership is retained. Its stem, options, correct answer/hash, feedback, hard difficulty, and all source provenance are identical to the approved before state; sourceChapter remains historical provenance.\n');
add('Registry and faculty-outcome coverage were regenerated only for the affected existing parent/derived concepts. Library, registry, manifest, outcome-policy and existing checksum references were refreshed. The normal comprehensive runner updated its generated preset counts by one where the accounting record changed concept. No validation assertion was relaxed.\n');
add('## Numerical / Economic Verification\n');
add('- **P62B-ELAS-B3-019:** the old record supplied no numeric price/quantity values. The revision explicitly gives positive fixed quantity and a price increase, without inventing numbers. Quantity percentage change is zero; midpoint demand elasticity is zero. With Q > 0 and P2 > P1, revenue change is Q(P2 - P1) > 0. Choice A is uniquely correct. All options and the key/hash remain unchanged. The finalBoss checkpoint role and hard canonical difficulty are retained; the diagnostic task is unchanged. No image was added.\n- **PM5-PC-BR-088:** cumulative variable costs at Q = 0, 1, 2, 3, 4 are 0, 3, 8, 16, 28. At P = $10, revenue less variable cost is 0, 7, 12, 14, 12, uniquely maximized at Q = 3. At P = $6 it is 0, 3, 4, 2, -4, uniquely maximized at Q = 2. Unavoidable fixed cost does not alter this ranking; producing two units contributes $4 toward it. Choice A is uniquely correct. The answer hash was recomputed because keyed option text changed.\n- **P62C-CPS-H-026:** accounting profit = 500 - 280 - 70 = **$150**. The $40 forgone income is implicit, so it is excluded. Choice D remains uniquely correct and its hash is unchanged.\n');
add('All three records have four distinct options, exactly one resolving answer hash, nonempty consistent feedback, preserved provenance and roles, and no remaining wording from the corrected exception.\n');
add('## Validation Results\n');add('Runner | Result\n--- | ---');for(const r of runners)add(`${r.runner} | ${r.status}`);add();
add('**29/29 active Composer runners passed. 14/14 exporter tests passed.** Canonical integrity: 149 concepts, 9,779 unique questions, 1,133 graph-required question IDs, 507 assets. Content scope, faculty outcomes, question quality, comprehensive lineage, checkpoint/remediation, graph sync, mode availability, and Trial by Graph all passed.\n');
add('The existing historical adapter now layers an explicit three-ID approved state over the unchanged consolidated-cleanup ledger, validating the immutable pre-closure source and exact before/after fields and storage locations. Focused negative checks rejected an unauthorized mutation on each of the three target IDs and on an untouched ID. No global validation was weakened.\n');
add('Evidence: `tmp/microeconomics_exception_closure/active_suite.log`, `exporter_tests.log`, `target_checks.json`, `publication.json`, `export_checks.json`, and `hash_comparison.json`. Reproducible closure tools: `audit_tools/microeconomics_exception_closure/apply.cjs` and `verify.cjs`.\n');
add('## Projection Counts\n');add('Projection | Before | After\n--- | ---: | ---:\nMicro | 6,301 | 6,301\nGeneral | 1,589 | 1,589\nMacro | 4,745 | 4,745\nGlobal canonical IDs | 9,779 | 9,779\n');
add('Full ID sets match the pre-closure baseline in all three course areas. None of the three targets appears in General; every General record and all previously approved General decisions remain unchanged.\n');
add('## Export Verification\n');
add(`Normal exporter regenerated all three CSV/PDF pairs. Micro CSV: **6,301 rows, 6,301 unique IDs, ${exportsCheck.csv_columns} columns, zero canonical mismatches**. General and Macro companion CSVs also have zero canonical mismatches and their expected 1,589 / 4,745 rows.\n`);
add(`Micro PDF: **6,301 IDs exactly once in expected order**, ${exportsCheck.pdf_pages.toLocaleString('en-US')} pages, all current questions/options/keys/feedback verified by the exporter. All pages rendered successfully; zero out-of-page text bounds, blank pages, replacement characters, answer-marker errors, split question cores, or missing-image pages. All ${exportsCheck.pdf_links} internal links resolve and ${exportsCheck.pdf_outline_entries} top-level outline entries are present.\n`);
add('Target PDF pages: '+expected.changedQuestionIds.map(id=>id+' — '+exportsCheck.question_pages[id]).join('; ')+'. The elasticity page has no absent-graph reference, the bridge shows the new output application, and the accounting page shows current cost/profit metadata. Rendered target pages and representative document/navigation pages were visually inspected; no clipping or overlap was found.\n');
add('Student publication succeeds with **6,276 canonical IDs**, exactly matching the pre-closure publication ID set. All three revised records publish with current stem, options, key and feedback. All ten requested modes pass; the generated inline JavaScript compiles. The 25 pre-existing Market Failures publication omissions remain unchanged.\n');
add('## Final Status\n');
add('**FINAL STATUS: PASS**\n');
add('MFV-001, the MEA-0078 / PM5-PC-BR-088 remaining exception, and MFV-003 are CLOSED. Remaining question-level exceptions: **0**. No new ambiguous key, economic error, routing regression, or shared-General regression was introduced.\n');
add('The previously verified MODE-AVAILABILITY, LEGACY-PUBLICATION, and PDF-METADATA-CONTINUATIONS limitations remain unchanged and nonblocking. The factor/choice/inequality preset still has no ordinary Legendary inventory, the 25 legacy publication omissions remain, and the seven metadata-continuation layout notes were not treated as defects.\n');
add('Hash comparison of 14,662 protected/tracked files confirms the original Micro audit, consolidated-cleanup report and ledger, final-verification report/evidence, and all unrelated tracked content are preserved. Only the authorized source/derived files, narrow historical adapter, existing checksums, and generated validation result changed. This closure adds its own report and ledger; no Macroeconomics audit was started.\n');
const dest=path.join(root,'faculty_exports/audits');fs.writeFileSync(path.join(dest,'microeconomics_exception_closure.md'),lines.join('\n'));fs.writeFileSync(path.join(dest,'microeconomics_exception_closure_changes.json'),JSON.stringify(ledger,null,2)+'\n');console.log(JSON.stringify({status:'PASS',closed:changes.map(c=>c.id),reports:2}));
