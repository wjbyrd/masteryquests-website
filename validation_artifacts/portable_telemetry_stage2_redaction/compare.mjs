import fs from 'node:fs';import path from 'node:path';import {createHash} from 'node:crypto';
const out='validation_artifacts/portable_telemetry_stage2_redaction',base=JSON.parse(fs.readFileSync(out+'/baseline/source.json'));const changed=[],missing=[];for(const [p,h]of Object.entries(base.hashes)){if(!fs.existsSync(p)){missing.push(p);continue;}if(createHash('sha256').update(fs.readFileSync(p)).digest('hex')!==h)changed.push(p);}fs.writeFileSync(out+'/source-comparison.json',JSON.stringify({baselineFiles:Object.keys(base.hashes).length,changed,missing},null,2)+'\n');console.log(JSON.stringify({changed,missing}));
