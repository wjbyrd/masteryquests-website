# Authoring and publication trace

Authoritative inspected checkout: C:\Users\Jennings\Documents\GitHub\masteryquests-website.

1. `build/faculty-build-composer/data/concept-reviews/concept_review_source.json` contains 151 current logical records. The checked instructional text fields match the current corrected Composer PDF text. Historical calibration/editorial/pedagogy/full-library JSON and PDFs are retained snapshots, not alternative source roots.
2. `build/faculty-build-composer/data/concept-reviews/manifest.json` is the current Composer integration manifest, with per-resource code, canonical concepts, filename, checksum, size, page count and routing disposition. `composer-core.js` validates the manifest and resolves selected concepts into required review resources. `composer.js` reads the current manifest and copies source bytes into generated packages. The runtime's public fallback base is `https://masteryquests.org/concept-reviews/`. Public root PDFs have no separate `concept-reviews/manifest.json` in this checkout.
3. `tools/build_concept_review_manifest.py` under the Composer directory reads the current source, current PDF directory, validation metadata, Composer library and course-area model. It accepts explicit root and output paths. It inspects text/language/pages/checksums but is not an accessibility conformance validator. It was inspected, not executed here, because no active PDF bytes were installed.
4. `tools/complete_macro_concept_reviews.py` supports an explicit Composer root, staging output, selected review codes, and publication switches. Its `publish_sources` reconstructs records from its Python constants. It draws 20 later Macro sheets with standard Helvetica fonts. `tools/expand_micro_concept_reviews.py` draws 15 later Micro sheets and updates the canonical source plus a historical production-source copy; its main path writes active output directly. Neither generator emits the required semantic tree in the inspected implementation. Neither was run during this task. Their root/output paths were inspected before deciding against execution.
5. The original 116 Arimo-font sheets are present with matching current JSON, but their complete original renderer was not located among the tracked PDF tools. Existing later production/correction outputs were not regenerated through a historical renderer. Any future restoration must be verified against current corrected text and images, not treated as authoritative based on its filename or date.
6. MACRO-23 and MACRO-24 use the current dedicated `assets/quantity-theory-fixed-v-y.png` under the Composer Concept Review directory. Current source metadata records SHA-256 `9a647b1312131cb74ce5a12f89f7ca18db35bb7101d077224f6d80626c55b31d`. The pilot additionally pins the decoded image stream as actually displayed. Current corrected fixed-V/Y wording, monetary values, and reciprocal-price arithmetic are preserved. No question-bank graph was changed or substituted.
7. Public copies exist for 136 records and are byte-identical to their Composer counterparts. MICRO-54 through MICRO-68 have Composer PDFs but no expected public copy. This is a baseline public-fallback gap, not a candidate installation failure. No missing public file was filled with an unvalidated candidate.
8. A current source scan found 13 built-in game pages with dormant Concept Review UI. Each explicitly sets `conceptReviewResult` to kind NONE with empty recommendations/choices. Those matches do not establish active use of shared PDFs. No direct shared-PDF reference was confirmed in the frozen built-in Macro/Micro games scanned. Composer-generated resource packages and public fallback are the confirmed consumers. Separately distributed/generated games outside the checkout were not inspected. All referring-file evidence and unresolved dynamic slide paths are in `public_pdf_references.json`.

The new pipeline is an experimental source-aware postprocessor, not a replacement full renderer. It freezes the source/PDF pair with hashes, maps exact meaningful text objects to source fields, adds a real logical tree, validates displayed graph fingerprints, and writes only task staging. `accessibility_semantics.json` is its canonical pilot metadata; descriptions and formula alternatives must not be copied into multiple unrelated authoring files.

The standalone accessibility gate is intentionally red and is not yet integrated into the maintained generation/install commands. Existing generators can still emit untagged PDFs. A complete future fix must connect a proven accessible renderer/postprocessor and independent validation gate to full builds, targeted corrections, packaging and installation, then refresh checksums and install identical bytes into both active locations. Do not advertise the present partial prototype as regeneration-safe production remediation.
