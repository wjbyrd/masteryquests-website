import fs from 'node:fs'; import {loadComposerLibrary,collectComposerQuestions,auditQuestionRecords,auditQuestionConstruction} from '../../audit_tools/question_quality_auditor.mjs'; import {createRequire} from 'node:module'; const req=createRequire(import.meta.url);const lib=loadComposerLibrary();const ids=new Set(Object.keys(JSON.parse(fs.readFileSync('tmp/microeconomics_audit/projection.json','utf8'))));const entries=collectComposerQuestions(lib).filter(e=>ids.has(e.id));const map=new Map(lib.assetInventory.map(a=>[a.runtimePath,a]));const result=auditQuestionRecords(entries,{assetMap:map});result.construction=auditQuestionConstruction(entries); req('../../build/faculty-build-composer/tests/composer-integrity-contracts.js').assertCanonicalIntegrity(lib);fs.writeFileSync('tmp/microeconomics_audit/automated_candidates.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result.counts));