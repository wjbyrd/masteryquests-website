# Reviewed visual authoring inputs

These six original-layout PDF sources were copied byte-for-byte from the current authorized Composer resources during the eight-document accessibility pilot. Their SHA-256 identities and instructional source-record hashes are in the canonical `../accessibility_semantics.json`. They preserve the current corrected graph and economics content.

The source-aware accessibility build reads these inputs, not the eventual active tagged outputs. They contain the original visual painting programs and are intentionally untagged source inputs. They are not release candidates. Do not copy them into public or Composer active PDF locations. The source/generator lifecycle must transform and validate them first.

No maintained original-layout renderer was found. Future visual/content corrections must update the instructional source and these visual inputs coherently, followed by explicit semantic review and fingerprint renewal. Mismatches fail closed. The later-layout family uses its maintained Python generators instead of these visual inputs.
