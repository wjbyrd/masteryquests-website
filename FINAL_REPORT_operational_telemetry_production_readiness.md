# Operational telemetry production readiness audit

## Task Identity

OPERATIONAL_TELEMETRY_PRODUCTION_READINESS_V1. Audited 2026-09-12. Result: **READINESS BLOCKED — SPECIFIC FOLLOW-UP REQUIRED**. The pipeline works; the requested fail-closed faculty enablement and universal public/local-only claims are not established by current code. No implementation or policy was changed.

## Operational Purpose

This is product/instructional operational analytics, not a research-study telemetry system. Its purposes are improving games/content and understanding aggregate usage, missed questions, concepts and objectives. Research/IRB workflows are separate and impose no additional activation gate in this report. Behavioral observations are contextual instructional signals, not automatic cheating detection.

## Repository

Working directory and Git top-level: `C:\Users\Jennings\Documents\GitHub\masteryquests-website`. Branch `main`; HEAD `6a5bbba835fcc9ce2b3c212e57580563db52b43d`. Initial Git status clean. Write access verified by creating this audit's evidence directory and files. No AGENTS.md was found in the repository file inventory. The forbidden alternate workspace was not accessed. No reset, commit or push occurred. Existing tracked files remain unchanged.

## Current Architecture

| Component | Current source and behavior |
|---|---|
| Private client | `play/managerial-directorate-telemetry-poc/telemetry-client.js`; classroom client generated by `audit_tools/managerial_classroom/build.mjs`. Hooks existing engines; keeps local CSV and a separate bounded outbound queue. |
| Public client | `play/managerial-intelligence-directorate/local-telemetry.js`, generated from maintained parity/contract sources; no transport. Composer template also local-only. |
| Worker | `server/anonymous-telemetry-poc/worker.mjs`, name `masteryquests-anonymous-telemetry-poc`, compatibility date 2026-08-07, workers_dev false. |
| Route | Locally declared `masteryquests.org/api/anonymous-telemetry-poc/*`; `/v1/health`, `/v1/events`, `/v1/admin/*`, prefixed or unprefixed in handler. No www route declared; www is an allowed Origin. |
| D1 | Binding TELEMETRY_DB; database `managerial-telemetry-poc`; ID `16b248ff-20cb-429c-a4a1-dd50d17c1fd0`; four tables in migration 0001. Migration 0002 repairs historical completion semantics and is not a new schema requirement. |
| Admin | Worker summary, anomalies, run read/reconstruct, CSV, governance; `audit_tools/telemetry_governance/admin.mjs` for policy/export/maintenance. |
| Faculty toggle | Source constant REMOTE_COLLECTION_ENABLED=true in both private builders; generated HTML meta; separate browser opt-out checkbox. No maintained Composer faculty control named ALLOW DATA COLLECTION was found. |

The website Worker `masteryquests-website` and classroom-access gate are separate from the anonymous ingestion Worker. A protected game page does not make ingestion authenticated.

## Private-Build Functional Proof

**FUNCTIONAL PIPELINE ALREADY PROVEN.** Historical local synthetic evidence in `validation_artifacts/telemetry_governance_activation` records backend 16/16, private browser 29/29, public browser 103/103, download/opt-out 17/17 and scheduled 11/11. These are historical local results, not evidence of current remote code.

Fresh audit checks: governance 14/14; scheduled retention 11/11; measurement contract 16/16; real browser/download/control checks 17/17. Governance renderer and measurement generator check modes passed. Fresh artifacts are in [operational_telemetry_readiness](validation_artifacts/operational_telemetry_readiness/). Browser tests intercept nonlocal traffic and use synthetic local Worker/SQLite. They exercised all four games in public, classroom and POC families, generated Composer, actual downloads, refresh, opt-out cancellation/re-enable, and storage failure. No browser/Worker exceptions were reported.

Current mapping supports run start/resume, answer submission/evaluation, sequence IDs, terminal-only completion, incomplete reconstruction, raw/accepted attempt distinction, wall/visible/unfocused timing, tab transitions, selection/copy counts, mode/question/tag/type/objective context, wagers/fading, summary and CSV. Reconstruction is observation of received events: dropped events and interrupted collection can make a run appear incomplete. POC safeOverrides omits the four wager/fading numeric extras that the classroom generator explicitly adds; do not claim every optional field has producer parity.

## Faculty Enablement Audit

**BLOCKER B1 — missing/invalid configuration fails open.** `remoteEnabled()` at private client line 19 rejects only exact meta value `disabled`, memory opt-out, or stored value `1`. With accessible storage, missing meta, invalid meta, missing stored preference and malformed stored preference allow transmission. Extracted-function execution reproduced all six states in `audit-results.json`. This conflicts with the requested missing/invalid fail-closed requirement.

Both private builders deliberately set REMOTE_COLLECTION_ENABLED=true (POC line 6; classroom line 7), and all eight generated game pages declare enabled. This is explicit build configuration, not evidence that a faculty member chose a setting in Composer. A pristine load with an empty queue has nothing to send immediately, but gameplay sends without a new click and a persisted queue can flush 250 ms after loading. Inclusion of the adapter with missing configuration would also enable it.

| State/scope | Verified behavior |
|---|---|
| Enabled | Emission and flush permitted; disclosure displays enabled state. This is a browser/deployer switch, not an authenticated faculty authority check. |
| Disabled build | Exact disabled meta prevents checkbox/API re-enable. Local recording/export continues. |
| Browser disabled | Checkbox “Send future anonymous gameplay events” calls setRemoteCollection(false); clears unsent queue, retry timer and sent-manifest cache, counts cancellation, persists preference. Re-enable does not replay cancelled events. |
| In-flight requests | Not recalled by toggle; epoch check ignores their response. Already accepted data remains under normal retention. |
| Persistence | HTML build meta plus namespaced localStorage remoteDisabled:v1, with in-memory suppression. Accessible but cleared storage restores enabled build default. Storage access throwing fails closed in remoteEnabled(). |
| Scope | Browser/profile/origin and build-family namespace, across that family's four games; not per course/faculty/run. Two build families have distinct namespaces. |
| Disclosure recording | Notice includes mq-disclosure/2, mq-governance/2 and 730-day policy; browser state persists. There is no event field establishing faculty authorization or historical disclosure acknowledgment. Export policy sidecar describes export-time policy only. No new field is proposed. |

**IMPORTANT BEFORE ACTIVATION:** resolve the requested faculty workflow versus the actual source-level control explicitly. Preserve faculty opt-in policy; do not label the existing browser checkbox as an authenticated faculty toggle. Composer has no central-collection option/endpoint in the inspected current implementation.

## Centralized Data Inventory

The complete per-field inventory is an integral appendix: [FIELD_INVENTORY.md](validation_artifacts/operational_telemetry_readiness/FIELD_INVENTORY.md). It enumerates all **81 accepted event-level input fields**, source/purpose, current necessity, linkage and retention, the exact **81-column admin CSV**, current **87-column Managerial** and **116-column Composer** local CSVs, legacy sets and definitions. Inputs and CSV columns are different sets despite equal counts. Base fields map to SQL snake case; extras are retained in extras_json. selectionReason, weaknessEstimate and sourceEvent are exposed within extras_json rather than individual CSV columns.

Additional complete storage/transport structure:

| Object | Every field / purpose |
|---|---|
| Request wrapper | phase, events[]; routing/schema envelope. Header x-telemetry-phase repeats phase; content-type JSON. No application cookie sent (credentials omit). |
| telemetry_events | All 37 normalized inputs mapped to SQL, extras_json containing approved extras, plus server-default received_at. Every field exported; receipt is operational arrival/retention authority. |
| telemetry_runs | run_id, anonymous_client_id, build_id, build_version, game_id, mode: grouping/provenance; first_event_at, last_event_at: client event range; first_received_at, last_received_at: server receipt range; event_count, max_sequence: coverage; completed, completion_status: terminal state; synthetic: test classification. Whole-run retention. |
| telemetry_ingest_batches | batch_id (server UUID), received_at, anonymous_client_id, event_count, inserted_count, duplicate_count, synthetic. Receipt/idempotency diagnostics; each receipt ages independently under retention. Run deletion cannot identify and erase individual batch receipts. |
| telemetry_rate_limits | anonymous_client_id, window_minute, event_count. Short-window abuse accounting; probabilistic cleanup after ten minutes, plus retention purge. Persistent client key is reused. |
| Response only | ok, phase, batchId, accepted, duplicates, acknowledgedEventIds. Client delivery diagnostics; not additional student measurements. |
| Manifest JSON | manifestSchema, contractID, measurementRevision; buildRevision, engineRevision, trackerSourceRevision, assetRevision, contentRevision, configurationRevision; mode, modeParameters, configuration. Artifact/config hashes, not device fingerprints. |
| Manifest configuration | recipeSchema, selectedConceptIds, contentScopes (concept-key -> preset, outcomeIds), policyFingerprint, samplingStrategies (quiz, trialGraph, fadingFortune, riskReward), supportedModes, dailyChallengesEnabled, checkpoints (checkpointOne, checkpointTwo, finalCheckpoint), fadingIntervals (easy, medium, hard, elite, legendary), startingBankroll, wagerRatios. Only allowlisted analytical settings; faculty title/name excluded. |
| modeParameters | quizTarget, trialGraphTarget, timedLimitMs, fadingTarget, riskTarget. Mode denominators/configuration. |
| supportJSON | hintDisplayed, artifactApplied, artifactRemovedCanonical[], modeRemovedCanonical[], remainingOptionCount. Assistance state without content. |
| deliveryQuality | overflow, permanentlyRejected, cancelled, storageFailures; validator additionally accepts localLastSequence, localRetainedRows, knownMissingPrefix, remoteOverflow, remoteCancelled, remotePermanentlyRejected, remoteStorageFailures, remotePending. Coverage limitations, not behavioral judgments. |
| weaknessEstimate | Up to five rows of key, attempts, accuracy. Engine aggregate context. |
| Export sidecar | format, governancePolicyVersion, disclosureVersion, measurementContract, exportSha256, exportedAt, scope (buildId, includeSynthetic, rowLimit), retention (days, mode, unit, clock), applicability. Server policy/export custody context, outside D1 lifecycle. |

Private local storage additionally holds the queue (all unsent event fields, cap 2,000), client UUID, source-run-to-central-run mappings, per-run sequence counters, active run by game, quality counters and remote-disabled preference. POC has debugFailure. Engine-local rows use the appendix columns; contract state retains manifests, presentation/attempt references, sequence/quality and support state to preserve resume/export provenance. Gameplay saves/profile names, leaderboard names, preferences, progress, answer state and question objects are local gameplay state, not blindly uploaded. Local verification codes and source run identifiers can link a deliberately submitted local file to centralized records. Legacy local exports preserve unknown original columns; no finite inventory can promise that arbitrary pre-existing browser data lacks extra fields.

## Privacy / Identifiability Review

**HIGH PRIORITY / IMPORTANT BEFORE ACTIVATION I1:** anonymousClientId is a persistent pseudonym. getClientId() stores/reuses a UUID without expiry in localStorage; D1 indexes it in events/runs and stores it in batches/rate windows. It is not a student name, but links multiple runs and all games in one build family. No course boundary exists. Reusing the family for multiple courses can link them. Governance/disclosure already acknowledges random browser run linkage, so this is not silently classified as a frozen-contract violation; the stronger requested exclusion of persistent cross-course identity is not proven and needs an owner scope decision. No field was removed.

For the anonymous Worker, maintained clients intentionally exclude name, Troy ID, SIS ID, email, Canvas user ID, IP address, device fingerprint, advertising ID, free-text answers, copied/selected content, clipboard contents, keystrokes and unrelated-tab destinations. selectedResponse is numeric; selection/copy records contain counts/flags/intervals only. sourceRunId and exact timestamps support indirect linkage, especially if joined with named submissions or small class groups. Arbitrary supplied values in permitted string fields can still contain identifying text; validation cannot prove semantic anonymity. “No identifying information exists anywhere” is not supported.

## Provider Boundary

**VERIFIED FROM CODE:** anonymous application payload/storage exclusions above; no IP extraction or user-agent field in the anonymous Worker; bounded scheduled summaries omit event/run/client IDs, payloads and credentials. General fetch failures log raw Error objects on 5xx, so provider error-log content is not proven as tightly bounded as scheduled logging.

**VERIFIED FROM CONFIGURATION:** declared routes, binding, origins, rate and retention values, cron. The local file does not specify observability/log retention, account access or backups.

**OWNER / PROVIDER CHECK:** request/network IP metadata, security/access logs, Worker logs/analytics/traces/Logpush, connected products, retention/access of those records, D1 backups/Time Travel and downstream copies. Application deletion cannot erase those copies. The qualified claim is: Mastery Quests anonymous application telemetry does not intentionally store direct participant identifiers, but does store a persistent browser pseudonym. This claim does not extend to the legacy National Engine path below.

## Public / Default Build Safety

Public Managerial and default generated Composer: **NO ISSUE** for anonymous transmission. Current browser tests confirm local downloads, no centralized ingestion for these public/default targets. Shared public adapter and Composer template have no fetch/sendBeacon/XMLHttpRequest transport. A scan of 547 tracked HTML/JS/JSON files under play/games/Composer finds anonymous endpoint/client references only in the 11 private integration files listed in audit-results.json. No anonymous transport appeared in polished Micro/Macro shared defaults.

**IMPORTANT BEFORE ACTIVATION I2 — HIGH PRIORITY legacy exception:** `play/macro-command-system/national-engine/index.html:5320` and `nationalengine.html:5190` define sendGameData that posts to a signed Power Automate endpoint. The index's later wrapper at line 5770 calls the original except during resume bootstrap; it does not disable networking. Payload fields are timestamp, runID, token, version, telemetryVersion, gameVersion, userAgent, game, event, user, email, room, questionId, correct, tag, type, objective, responseTime, attempt, streak, elapsedTime, adaptiveMode, totalTime, accuracy, maxStreak, totalAttempts, verificationCode. user/email directly identify; userAgent and IDs aid linkage. Index also saves username/studentEmail locally. Its local-telemetry version label does not neutralize the POST. No endpoint token/value is reproduced here.

This is outside the anonymous D1 pipeline and was inspected read-only, not executed. Live reachability/publication and recipient storage/retention are unverified. Local asset exclusions inspected do not explicitly exclude these files. Therefore a blanket “all public/default games are local-only” statement is false for repository code; owner must establish release exclusion or authorize separate remediation of this protected legacy path. Polished Micro and other inspected Macro files have no matching fetch/sendBeacon/XMLHttpRequest calls; this static evidence is not a full browser-network proof for every historical artifact.

## Frozen Contract Verification

**NO ISSUE:** mq-measurement/1, mq-governance/2, mq-disclosure/2 remain canonical. Measurement release: mq-tracker-2026.09.10-contract1, mq-local-csv/1, envelope 3, mq-run-manifest/1. Generator/renderer checks passed. Historical envelopes 1/2 remain accepted with legacy semantics. No bump or redesign. Historical documentation mentions research in places (including validator error text); this does not turn operational retention into IRB policy. Enablement fail-open behavior is a mismatch with this audit's requirement, not hidden evidence of a changed frozen contract.

## Operational Retention

**NO ISSUE:** 730 * 86,400,000 milliseconds before trusted invocation Date.now(), rendered canonical UTC ISO. Exactly 730 elapsed 24-hour days, not calendar years, client timestamps or an anniversary promise. r.last_received_at must be strictly before cutoff; any event with invalid or at/after-cutoff received_at protects the entire run. Invalid run receipt is not guessed into eligibility. Recent/straddling runs retain old prefixes, including incomplete runs. Duplicate-only uploads do not extend last stored run receipt; batch receipts have their own clock.

Preview counts run/event eligibility; execute re-evaluates inside a D1 batch, deletes selected events then empty eligible run summaries, plus expired batch/rate data. A late new event protects the run; atomic failures roll back. Local transaction, overlap/idempotency and boundary checks passed. D1 transactional semantics rely on the provider [batch API](https://developers.cloudflare.com/d1/worker-api/d1-database/); production scale/runtime has not been load-tested. Preview auxiliary counts are separate reads and not necessarily the same snapshot as run counts. Orphan events are removable by explicit run UUID, but rolling selection begins with run summaries. Retries after deletion can recreate a run. These are documented limits, not policy changes.

## Scheduled Retention

**LOCAL CONFIG READY. PRODUCTION TRIGGER VERIFICATION REQUIRED.** wrangler declares `17 4 * * *`, daily 04:17 UTC. Worker scheduled -> scheduledRetention -> configuration/cutoff -> govern preview and execute, using TELEMETRY_DB. Expected cron, positive safe-integer nonfuture scheduled time, valid current clock and exact approved vars are required. Failure logs RETENTION_FAILED, emits bounded operation counts and rejects. No plaintext secret or request-supplied bypass is used; neither token is needed internally. Removing manual secrets does not stop this schedule. Audit logs themselves depend on provider observability; no new D1 audit table exists.

## Manual Deletion / Maintenance

All paths below are under /api/anonymous-telemetry-poc; all need admin, and POST maintenance also requires distinct maintenance header even for preview.

| Endpoint/tool | Scope and guard |
|---|---|
| POST /v1/admin/retention; admin.mjs retention | Default dry-run; execute requires explicit reviewed cutoff no newer than policy and confirm PURGE_EXPIRED_RUNS. Whole eligible runs, plus aged auxiliary tables. |
| POST /v1/admin/delete-run; admin.mjs delete-run --run-id UUID | UUID preview; execute confirm DELETE_RUN:lowercase-UUID. Deletes all events with UUID, including orphan events, then run summary. Does not erase batch receipts. |
| POST /v1/admin/cleanup | Bounded build ID or synthetic-only whole-run selection; execute confirm DELETE. Mixed builds/synthetic runs protected. This is broad maintenance, not ordinary retention or recommended smoke cleanup. |

Unknown/cross-scope body keys rejected; 2,048-character parsed body limit, actions constrained; no caller SQL. SQL values parameterized. Explicit run/build cleanup can intentionally delete recent runs; it is not bound by retention cutoff, and must not be represented as such. No production delete or preview executed.

## Authorization Separation

**NO ISSUE FROM CODE:** ADMIN_TOKEN bearer required on every admin path. MAINTENANCE_TOKEN in x-telemetry-maintenance additionally required for maintenance and must differ from ADMIN_TOKEN. Missing admin gives 401; missing/wrong/equal maintenance gives 403. Both expected as Worker secrets; values never included in report. Production presence/distinctness requires safe owner verification; a secret-name list can establish presence but not distinctness. Scheduled internal authority has no public route. Shared tokens provide no person-level attribution.

## CORS

Explicit exact equality against comma-split ALLOWED_ORIGINS: https://masteryquests.org and https://www.masteryquests.org. Approved OPTIONS yields 204, echoed Origin/Vary and allowed POST,GET,OPTIONS and content-type,x-telemetry-phase,authorization headers. Unapproved Origin on OPTIONS/POST is 403. No credentials response header; browser POST omits credentials. **IMPORTANT BEFORE ACTIVATION I3:** absent Origin is accepted, and callers can forge an approved Origin. CORS is a browser restriction, not ingestion authentication. Admin responses do not expose cross-origin reads; x-telemetry-maintenance is absent from allowed preflight headers, so maintained Node CLI is the supported operator path. Error responses from outer catch lack CORS headers, potentially obscuring status to cross-origin browsers.

## Rate Limiting

Current local limit 300 submitted events per anonymous client per minute, minimum configured limit 50. SELECT then UPSERT in D1 is not an atomic quota reservation: concurrent requests can overshoot; changing client UUID bypasses the scope. Non-numeric configuration yields NaN and defeats comparison. Present config is valid. Exceeding limit returns 429; client retains queue and retries exponentially up to 30 seconds (10-second request timeout); cap 2,000 records, overflow counted. Duplicate retries consume quota. One answer can generate multiple events, so 300 is not 300 answers; multi-tab/rapid synthetic play can hit it. Old rate windows probabilistically purge at 1% of requests after ten minutes and also via retention. **IMPORTANT BEFORE ACTIVATION I3:** confirm bounded pilot volume and provider abuse/cost controls; no claim of a hard security boundary or production load proof.

## Event Validation

**NO ISSUE for basic structural validation, with limits:** 128 KiB actual UTF-8 body check plus content-length early check; JSON object and matching phase; 1–50 events; envelope schema 1/2/3; UUIDs; event-name regex; sequence 1..1e9; timestamps within prior 90 days/next 10 minutes; bounded numeric/context fields. Unknown event fields rejected; forbidden identifier key names recursively rejected. Envelope-level unknown keys are ignored rather than stored. Extras have nested allowlists; schema 3 requires contractID; manifests/configuration are hash-validated; timing components must agree within 2 ms. Extras size 16,384 chars legacy / 65,536 schema 3. Core booleans coerce with Boolean() and numbers/strings can coerce; strict type assurance applies to extras more strongly than legacy base fields. Event types are regex-valid rather than a fixed semantic enum. Not all contract fields are mandatory on every schema-3 event.

Parameter-bound inserts, primary event ID and unique run/sequence provide ordinary retry idempotency; duplicate IDs/sequence pairs within a batch rejected. Existing conflicts acknowledged as duplicates, not payload-compared. Gaps/out-of-order permitted for offline delivery and diagnosed later; reconstruction's sorted sequence report alone cannot establish arrival order (admin anomalies does that). No server authorization binds runId to client/build identity; identifiers/config are client assertions. Concurrent first insert races can overcount run summaries because an INSERT OR IGNORE is followed by unconditional run upsert. **NON-BLOCKING FOLLOW-UP:** use event-derived aggregates and inspect anomalies; this is not person-authenticated or tamper-proof analytics. CSV quotes fields but does not neutralize spreadsheet formulas in malicious permitted string values; import untrusted CSV as text, not automatically executed spreadsheet content.

## Provenance / Versioning

**NO ISSUE:** actual emitted buildVersion is MQContract.build().buildRevision (hash), not the retained human BUILD_VERSION constant. Registry and manifests distinguish engine, content, assets, tracker, measurement and analytical configuration; canonical selected index survives shuffle. Changes to these fingerprints are covered by passing tests. Mixed historical versions remain labeled; null/unresolved provenance is not invented. schemaVersion/gameId/buildId/phase persist centrally; governance/disclosure export headers/sidecar are current-policy context, not historical consent evidence.

## Local Student Transparency

**NO ISSUE for maintained Managerial/Composer:** real Download Game Data passed across all 12 Managerial targets plus a generated game, including refresh. Local fields remain documented in frozen dictionary and appendix. Disabling central collection cancels only transport; local export remains. Browser storage denial/overflow is observable where supported; no guarantee of indefinitely complete local history. Names in local gameplay/copy-results or a named submitted file must not be confused with the centralized numeric answer envelope. Students/recipients control downloaded copies; D1 deletion does not delete them.

## Dashboard Readiness

No dashboard built. **NON-BLOCKING FOLLOW-UP:** derive metrics from received events with contract/manifest/version partitions, exclude synthetic, track coverage and avoid small-group identification. No course identifier exists; do not invent cross-course student counts.

| Metric | Required fields / support | Aggregation and privacy concerns |
|---|---|---|
| Most-played mode | runId, mode, run_started/mode_selected; supported | One run, not event count; exclude synthetic/retries; browser ID is not unique students. |
| Runs by game | runId, gameId, buildId; supported | Unique runs and build partition; missing start/resume segments documented. |
| Completion rate | run_started, run_completed, completionStatus; supported with coverage | Missing terminal does not prove abandonment; separate observed incomplete/collection interruption. |
| Accuracy | answer_evaluated, correct, acceptedAttempt, attemptID; supported | Count committed outcomes once; separate raw/rejected/accepted, exam revisions, repeats and assistance. |
| Most-missed question | questionId, manifestID/contentRevision, correct; supported | Version and repeated exposure/attempt denominator; no question text in D1. |
| Topic/tag weakness | conceptId, correctness; supported | Local tag maps to central conceptId; not guaranteed unique ontology across builds. |
| Weakest objective | learningObjective, correct/acceptedAttempt; supported | Missing/changed labels and sparse groups; do not infer individual deficits. |
| Type performance | questionType, correct; supported | Partition modes, difficulty and assistance. |
| Wall/active response time | response_time_ms, activeResponseTimeMs, hiddenTimeMs; supported | Evaluate committed answer intervals only; null vs zero; censor interruptions/outliers; visible is not attention. |
| Behavioral engagement | tabSwitchCount, focusLossCount, copy/selection flags/counts and intervals; supported | Hidden/unfocused overlap; no destinations/content/intent; broad aggregates only. |

CSV is limited to 50,000 rows without pagination/truncation flag and may split a run. Future dashboards must not silently treat an export at the cap as complete. Admin summary reports all events and recent 50 runs, not finished aggregate dashboards. sourceRunId/persistent browser ID/precise timestamps should not appear in faculty aggregates unnecessarily.

## Cloudflare Verification Matrix

| Item | Repository/code | CLI without mutation | Owner/UI exact check |
|---|---|---|---|
| Worker/version | Name/main/config verified | Public health 200 only; deployment UUID unknown | Workers & Pages -> exact telemetry Worker -> Deployments: record active version, timestamp, environment, source hash/rollback version. |
| Route | Apex route declared | Endpoint reachable; actual route assignment unproven | Settings -> Domains & Routes: exact pattern, owning Worker and zone; distinguish www origin from route. |
| D1 | Name/UUID/binding/migrations verified locally | storage:true means binding truthy, not query/schema check | Bindings -> TELEMETRY_DB UUID; D1 database identity and applied migration history, table constraints; do not apply migration 0002 merely to inspect. |
| Cron | 04:17 daily handler/config ready | Not verified | Settings -> Triggers: exact daily cron; inspect invocation success/failure and bounded counts after propagation. |
| Secrets | Both distinct authorities enforced | Not verified | Names ADMIN_TOKEN, MAINTENANCE_TOKEN present; protected custodian process verifies distinctness; no value screenshots. |
| Vars | Approved origins, 300, 730, automatic, mq-governance/2 | Not verified | Compare each deployed nonsecret var to local file; confirm no env overrides. |
| Access | Sole initial custodian documented | Not verified | Account members/API tokens/SSO/MFA, D1/export access and least needed privileges. |
| Logs/security/analytics | No application IP field; no declared retention | Not verified | Worker observability, logs/traces/Logpush, WAF/security/access/analytics products: contents, destinations, retention, readers, deletion controls. |
| Backups/copies | Outside application purge | Not verified | D1 Time Travel/backups and export/third-party copies: windows, restoration/reintroduction, access/disposal responsibilities. |

No authenticated Cloudflare connector available. `npx --no-install wrangler --version` could not resolve/run Wrangler (npm EACCES network/cache restriction); no credentials were loaded from files and no authenticated metadata claim is made. CLI owner discovery commands are in the runbook. Provider checks remain explicit, not presumed failures or policy changes.

## Current Production State

Read-only GET at **2026-09-12 20:27:37 UTC** to `https://masteryquests.org/api/anonymous-telemetry-poc/v1/health` returned HTTP 200 and `{"ok":true,"phase":"phaseAnonymousTelemetryPOC-v1","storage":true}`. Initial restricted-shell and web-open attempts failed; an authorized network-capable shell retry succeeded. Service appears deployed/reachable. This does not prove which Worker version serves it, actual D1 identity/schema/readability, governance version, secrets, or cron. **OWNER VERIFICATION REQUIRED** for those items. No production POST, admin export, retention preview, data query or mutation occurred.

## Deployment Diff

An exact remote before/after diff cannot honestly be produced without active deployment/config metadata. The exact local target is identified above; do not substitute the older POC deployment report for current state.

| Category | Required action / no-change determination |
|---|---|
| CODE CHANGES REQUIRED BEFORE DEPLOYMENT | Resolve B1 in the maintained private adapter/build enablement path so missing/invalid config cannot opt in; demonstrate faculty control scope. No patch made here. Owner-approved fix should preserve frozen fields/contracts. |
| PRODUCTION CONFIGURATION / SECRET CHANGES REQUIRED | Conditional only: deploy approved Worker if live differs; ensure the three exact governance vars, existing origins/300, TELEMETRY_DB binding, daily trigger, and distinct named secrets. Unknown remote state prevents asserting which actually change. Provision only missing authorized secrets; no automatic rotation. Schedule deployment can delete eligible real rows on next tick. |
| NO CHANGE REQUIRED | Measurement/governance/disclosure versions, 730-day duration, whole-run algorithm, current CORS origin list, D1 schema, public Managerial/Composer transport. No speculative migration or dashboard. |
| OWNER UI VERIFICATION REQUIRED | Capture live version/config/trigger/secrets metadata and rollback snapshot; resolve persistent-ID course scope (I1), legacy National Engine public-release exception (I2), and pilot abuse/export handling (I3). Do not publish a universal local-only claim before I2 is resolved. |

Static disclosure/adapters must match the approved Worker; website release is separate and must preserve frozen games. No exact website deployment command is invented for an absent dist artifact. This report/runbook/evidence are audit deliverables, not a production bundle.

## Synthetic Smoke-Test Plan

Prepared, not executed. [PRODUCTION_ACTIVATION_RUNBOOK.md](server/anonymous-telemetry-poc/PRODUCTION_ACTIVATION_RUNBOOK.md) gives commands, ordered 15 checks, expected results, scope, cleanup and rollback. Fresh synthetic production rows cannot become 730-day eligible via normal ingestion; changing client timestamps does not change server receipt, and backdated >90-day events reject. Therefore use the existing isolated fixed-time synthetic scheduled fixture for eligibility/destructive proof, and observe the real production schedule with reviewed policy counts. Do not age real rows or call a nonexistent public scheduler endpoint. A production deletion-of-expired-synthetic proof requires separately scoped isolated fixture authority or existing eligible synthetic rows, not an unsafe all-data purge.

## Activation Runbook

`C:\Users\Jennings\Documents\GitHub\masteryquests-website\server\anonymous-telemetry-poc\PRODUCTION_ACTIVATION_RUNBOOK.md`. Conditional instructions only; blocked until B1 and necessary owner decisions are resolved. No runbook deployment or production smoke ingestion/deletion was executed.

## Blockers

**B1 — BLOCKER:** missing/invalid collection configuration enables transport. Required closure: fail-closed behavior demonstrated without changing faculty opt-in policy or measurement contracts; record explicit faculty/deployer enablement for the intended build. Existing opt-out regression success does not test or satisfy this requirement.

## Important Before Activation

**I1 — IMPORTANT BEFORE ACTIVATION / HIGH PRIORITY:** persistent anonymousClientId and source-run linkage; document scope and reconcile requested no cross-course persistent identity with actual build-family storage. No unilateral deletion/redesign.

**I2 — IMPORTANT BEFORE ACTIVATION / HIGH PRIORITY:** National Engine's legacy identifying POST prevents universal public-local-only assurance. Owner establishes exclusion from deployed public artifacts or separately authorizes protected-file remediation. Live endpoint activity is unknown; do not test it with student data.

**I3 — IMPORTANT BEFORE ACTIVATION:** public unauthenticated ingestion, bypassable/non-atomic per-client limiter, and untrusted CSV text require an explicit bounded activation/abuse/export operating decision. No speculative redesign performed.

## Owner / Provider Verification

**OWNER / PROVIDER CHECK:** matrix entries for deployed identity, route, binding, secrets, schedule, logs/backups/access and downstream copies. **VERIFY DURING ACTIVATION:** the 15 smoke checks and actual scheduler invocation. **NON-BLOCKING FOLLOW-UP:** aggregate query design, pagination/coverage, concurrency diagnostics, historical optional-field differences. **NO ISSUE:** frozen contract synchronization, local retention/deletion authority, maintained local transparency and public Managerial/Composer no-transmission checks.

## Protected Systems

No production deployment/resources/D1 rows/secrets/cron changed. No telemetry contract/schema, game engine, public Micro/Macro, banks, Concept Review, manuscript, or existing historical artifact changed. No dashboard built. Only new audit report/runbook/evidence files were written in the authorized repository. Forbidden workspace unused. Faculty behavior was traced in source, extracted execution, and real local browser opt-out tests. Field inventory and provider qualifications are explicit. The 730-day baseline is operational policy. Final checks: root/HEAD unchanged, tracked diff empty, git diff --check clean, final report reopened.

## Final Status

READINESS BLOCKED — SPECIFIC FOLLOW-UP REQUIRED
